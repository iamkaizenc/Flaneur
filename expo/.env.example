# Flâneur - Autonomous Social Media Agent
# Environment Configuration

# =============================================================================
# CORE SETTINGS
# =============================================================================

# Application Environment
NODE_ENV=development

# Base URL for OAuth callbacks and API endpoints
BASE_URL=http://localhost:3000

# LIVE_MODE controls real API calls vs simulation
# Set to "false" for development/demo (simulates all API calls)
# Set to "true" for production with real API integrations
LIVE_MODE=false

# Primary domain for OAuth callbacks
PRIMARY_DOMAIN=app.example.com

# Supabase (if using)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Notification emails
NOTIFY_EMAILS=

# Legacy DRY_RUN support (deprecated, use LIVE_MODE instead)
# DRY_RUN=true

# Publishing Window (24-hour format)
PUBLISH_START_HOUR=8
PUBLISH_END_HOUR=22

# =============================================================================
# OAUTH CREDENTIALS
# =============================================================================
# Get these from each platform's developer console
# Only required when DRY_RUN=false

# X (Twitter) OAuth 2.0 PKCE
X_CLIENT_ID=
X_CLIENT_SECRET=
X_CALLBACK_URL=
X_SCOPES="tweet.read tweet.write users.read offline.access"

# Meta (Instagram/Facebook) Graph API
META_APP_ID=
META_APP_SECRET=
META_CALLBACK_URL=

# LinkedIn OAuth2
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_CALLBACK_URL=

# TikTok OAuth2
TIKTOK_CLIENT_ID=
TIKTOK_CLIENT_SECRET=
TIKTOK_CALLBACK_URL=

# Telegram Bot API (no OAuth)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# =============================================================================
# SECURITY
# =============================================================================

# Encryption key for storing OAuth tokens (32 characters)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_32_character_encryption_key_here

# JWT Secret for user authentication
JWT_SECRET=your_jwt_secret_key_here

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable/disable ads (only shown to free users)
ADS_ENABLED=true

# Enable/disable IAP dry run mode for testing
IAP_DRY_RUN=true

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# AI Services (for content generation)
OPENAI_API_KEY=your_openai_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key

# Email Service (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Database (if using external database)
DATABASE_URL=postgresql://user:password@localhost:5432/flaneur

# =============================================================================
# PLATFORM LIMITS & QUOTAS
# =============================================================================

# Daily posting limits per platform (used when LIVE_MODE=true)
DAILY_LIMIT_X=5
DAILY_LIMIT_INSTAGRAM=2
DAILY_LIMIT_LINKEDIN=1
DAILY_LIMIT_TIKTOK=3
DAILY_LIMIT_FACEBOOK=2
DAILY_LIMIT_TELEGRAM=999

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Enable detailed logging
DEBUG=flaneur:*

# Disable telemetry
EXPO_NO_TELEMETRY=1

# =============================================================================
# DEMO SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env
# 2. Keep DRY_RUN=true for demo mode
# 3. Generate ENCRYPTION_KEY: openssl rand -hex 32
# 4. Set JWT_SECRET to any secure random string
# 5. Run: npm run dev
# 6. Open the app and explore the 5-minute demo flow

# For production deployment:
# 1. Set DRY_RUN=false
# 2. Configure OAuth credentials for each platform
# 3. Set up proper database and external services
# 4. Configure SMTP for email notifications